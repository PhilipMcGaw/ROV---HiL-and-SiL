# Configuration

Place simulator and bridge configuration here. Keep credentials and machine-specific addresses out of version control. NATS Core is the current integration boundary; older MQTT references in project documents are historical.
