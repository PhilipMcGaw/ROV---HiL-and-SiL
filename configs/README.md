# Configuration

Place simulator and bridge configuration here. Keep credentials and machine-specific addresses out of version control. MQTT is the current integration boundary; the older NATS references in the course notes are historical.
