# HiL/SiL tests

Tests will cover ROS package behaviour, MQTT bridge mappings, repeatable scenarios, and end-to-end command/telemetry flow. Tests must state whether they are SiL-only, require the Raspberry Pi, or require other hardware.
