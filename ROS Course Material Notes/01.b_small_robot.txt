Yes — that's the point I'd tackle next. Before bringing your existing ROV repositories into the VM, we should prove the ROS 2 ↔ Gazebo ↔ RViz pipeline with something deliberately tiny.
I'd make a minimal differential-drive robot, not an ROV yet.
What we're trying to prove

`````
             Gazebo Harmonic
                  │
             Simple robot
                  │
        ┌─────────┴─────────┐
        │                   │
   simulated sensors    wheel joints
        │                   │
        ▼                   ▼
      ROS 2              ROS 2
        │
        ▼
      RViz2
``````

The robot could have:
two driven wheels
a simple chassis
a simulated IMU
a simulated camera
optionally a lidar/depth sensor
And initially we only need it to drive around in Gazebo and publish ROS 2 topics.
Why not build the ROV immediately?
Because there are several independent technologies to validate:
VMware graphics
Ubuntu
ROS 2
Gazebo Harmonic
ros_gz
ROS topics
sensor simulation
RViz
If we jump straight to a six-thruster ROV, a failure could be anywhere.
With a tiny robot:

```````
keyboard
   ↓
ROS 2 cmd_vel
   ↓
Gazebo
   ↓
robot moves
```````

we have a very clear first success criterion.
Then we can progressively turn it into your actual HIL simulator:
Phase 1
Simple wheeled robot
        ↓
Phase 2
ROV body + 6 thrusters
        ↓
Phase 3
IMU + depth + heading
        ↓
Phase 4
Camera
        ↓
Phase 5
NATS bridge
        ↓
Phase 6
ROV Control
        ↓
Phase 7
Physical HID
And importantly, the physical HID idea you've just described can eventually control the simulated vehicle:
Physical HID
    │
    ▼
NATS
    │
    ▼
ROV Control
    │
    ▼
ROS 2
    │
    ▼
Gazebo
    │
    ▼
simulated ROV
    │
    ├── depth ───────► NATS ───► HID depth display
    ├── motor state ─► NATS ───► HID LEDs
    └── faults ──────► NATS ───► HID warning lamps
That is ultimately the architecture I'd aim for.
So yes: next I'd build the tiny robot inside your new Ubuntu VM, without touching the existing GitHub project. Once that works, we can use it as the foundation for the ROV simulation rather than debugging ROS installation and your application simultaneously.










can I have a guide to make a small robot?












Yes. I'd make the first robot deliberately boring: a small two-wheel differential-drive robot. This gives you a controlled way to learn the ROS 2/Gazebo relationship before we introduce the six-thruster ROV.
One caveat: many search results for "Gazebo ROS tutorial" are now Gazebo Classic tutorials, which are obsolete for our Jazzy/Harmonic setup. Gazebo Classic reached EOL in January 2025. Gazebo Simulator So I'll keep the guide aligned with ROS 2 Jazzy + modern Gazebo Harmonic.
Mini Robot — ROS 2 + Gazebo Harmonic
What we're going to build
The finished simulation will be:
                  Gazebo Harmonic
                       │
                 ┌─────▼─────┐
                 │           │
                 │   robot   │
                 │           │
                 └─┬───────┬─┘
                   │       │
                left      right
                wheel     wheel
                   │       │
                   └───┬───┘
                       │
                  differential
                     drive
                       │
                       ▼
                     ROS 2
                       │
                 /cmd_vel
                       │
                       ▼
                    teleop
We'll eventually add:
                  robot
                    │
       ┌────────────┼────────────┐
       │            │            │
      IMU         camera       lidar
       │            │            │
       └────────────┼────────────┘
                    │
                   ROS 2
This is already teaching you concepts we'll use for the ROV.
1. Create the ROS workspace
In your Ubuntu VM:
mkdir -p ~/ROV---HiL-and-SiL/ros2_ws/src
cd ~/ROV---HiL-and-SiL/ros2_ws

Check:
pwd

You should get something like:

    /home/skippy/ROV---HiL-and-SiL/ros2_ws

Make sure ROS is sourced:

    source /opt/ros/jazzy/setup.bash

Check:

    echo $ROS_DISTRO

Expected:

    jazzy


2. Create our robot package
We'll call the package:
mini_rover_description
This package will initially contain the robot's physical description.

    cd ~/ROV---HiL-and-SiL/ros2_ws/src



ros2 pkg create \
  --build-type ament_cmake \
  mini_rover_description
You now have:
ros2_ws/
└── src/
    └── mini_rover_description/
        ├── CMakeLists.txt
        ├── package.xml
        └── ...
3. Create the model directories

cd ~/ROV---HiL-and-SiL/ros2_ws/src/mini_rover_description

mkdir -p urdf
mkdir -p launch
mkdir -p config
So:

mini_rover_description/
├── CMakeLists.txt
├── package.xml
├── urdf/
├── launch/
└── config/




4. Create the robot description
For the first version we'll use URDF/Xacro.
Create:

urdf/mini_rover.urdf.xacro

The robot will consist of:
             chassis
        ┌───────────────┐
        │               │
        │               │
    O───┤               ├───O
        │               │
        └───────────────┘
          left       right
          wheel      wheel


The important concepts we're establishing are:
base_link
wheel links
wheel joints
inertial properties
collision geometry
visual geometry
These concepts will transfer directly to the eventual ROV model.
Gazebo's robot modelling documentation similarly starts with a simple two-wheeled differential-drive robot. Gazebo Simulator


5. Put this in mini_rover.urdf.xacro



<?xml version="1.0"?>

<robot xmlns:xacro="http://www.ros.org/wiki/xacro"
       name="mini_rover">

  <!-- Dimensions -->
  <xacro:property name="body_length" value="0.40"/>
  <xacro:property name="body_width"  value="0.30"/>
  <xacro:property name="body_height" value="0.12"/>

  <xacro:property name="wheel_radius" value="0.08"/>
  <xacro:property name="wheel_width"  value="0.04"/>

  <xacro:property name="wheel_separation" value="0.34"/>

  <!-- ======================== -->
  <!-- Base chassis              -->
  <!-- ======================== -->

  <link name="base_link">

    <visual>
      <origin xyz="0 0 0"/>
      <geometry>
        <box size="${body_length} ${body_width} ${body_height}"/>
      </geometry>
    </visual>

    <collision>
      <origin xyz="0 0 0"/>
      <geometry>
        <box size="${body_length} ${body_width} ${body_height}"/>
      </geometry>
    </collision>

    <inertial>
      <mass value="5.0"/>

      <inertia
        ixx="0.1"
        ixy="0.0"
        ixz="0.0"
        iyy="0.1"
        iyz="0.0"
        izz="0.1"/>
    </inertial>

  </link>


  <!-- ======================== -->
  <!-- Left wheel                -->
  <!-- ======================== -->

  <link name="left_wheel">

    <visual>
      <origin xyz="0 0 0" rpy="${pi/2} 0 0"/>

      <geometry>
        <cylinder
          radius="${wheel_radius}"
          length="${wheel_width}"/>
      </geometry>
    </visual>

    <collision>
      <origin xyz="0 0 0" rpy="${pi/2} 0 0"/>

      <geometry>
        <cylinder
          radius="${wheel_radius}"
          length="${wheel_width}"/>
      </geometry>
    </collision>

    <inertial>
      <mass value="0.5"/>

      <inertia
        ixx="0.01"
        ixy="0.0"
        ixz="0.0"
        iyy="0.01"
        iyz="0.0"
        izz="0.01"/>
    </inertial>

  </link>


  <!-- ======================== -->
  <!-- Right wheel               -->
  <!-- ======================== -->

  <link name="right_wheel">

    <visual>
      <origin xyz="0 0 0" rpy="${pi/2} 0 0"/>

      <geometry>
        <cylinder
          radius="${wheel_radius}"
          length="${wheel_width}"/>
      </geometry>
    </visual>

    <collision>
      <origin xyz="0 0 0" rpy="${pi/2} 0 0"/>

      <geometry>
        <cylinder
          radius="${wheel_radius}"
          length="${wheel_width}"/>
      </geometry>
    </collision>

    <inertial>
      <mass value="0.5"/>

      <inertia
        ixx="0.01"
        ixy="0.0"
        ixz="0.0"
        iyy="0.01"
        iyz="0.0"
        izz="0.01"/>
    </inertial>

  </link>


  <!-- ======================== -->
  <!-- Left wheel joint          -->
  <!-- ======================== -->

  <joint name="left_wheel_joint"
         type="continuous">

    <parent link="base_link"/>
    <child link="left_wheel"/>

    <origin
      xyz="0 ${wheel_separation/2} 0"
      rpy="0 0 0"/>

    <axis xyz="0 1 0"/>

  </joint>


  <!-- ======================== -->
  <!-- Right wheel joint         -->
  <!-- ======================== -->

  <joint name="right_wheel_joint"
         type="continuous">

    <parent link="base_link"/>
    <child link="right_wheel"/>

    <origin
      xyz="0 ${-wheel_separation/2} 0"
      rpy="0 0 0"/>

    <axis xyz="0 1 0"/>

  </joint>

</robot>




This is intentionally simple.
We're not yet adding Gazebo plugins.
First we want to prove that ROS can generate the robot description.

6. Check the Xacro

    cd ~/ROV---HiL-and-SiL/ros2_ws
Build:
    colcon build
Source the workspace:
    source install/setup.zsh
Now generate the URDF:
ros2 run xacro xacro \
src/mini_rover_description/urdf/mini_rover.urdf.xacro \
> /tmp/mini_rover.urdf
Check the resulting file:
head /tmp/mini_rover.urdf
If that works, we know the Xacro is valid.
7. Install robot-state-publisher
sudo apt install ros-jazzy-robot-state-publisher
This package will eventually publish the robot's TF transforms.
The conceptual relationship is:
Robot description
       │
       ▼
robot_state_publisher
       │
       ▼
      TF
       │
       ├── base_link
       ├── left_wheel
       └── right_wheel
That becomes very important later for your ROV.
8. Add a launch file
Create:
launch/display.launch.py
Use:
from launch import LaunchDescription
from launch_ros.actions import Node
from launch.substitutions import Command
from launch_ros.parameter_descriptions import ParameterValue


def generate_launch_description():

    robot_description = ParameterValue(
        Command([
            "xacro ",
            "src/mini_rover_description/urdf/mini_rover.urdf.xacro"
        ]),
        value_type=str
    )

    return LaunchDescription([

        Node(
            package="robot_state_publisher",
            executable="robot_state_publisher",
            parameters=[
                {
                    "robot_description": robot_description
                }
            ]
        )

    ])
We'll improve this later; right now it's just a learning exercise.
9. Build again
cd ~/ROV---HiL-and-SiL/ros2_ws

colcon build

source install/setup.bash
Then:
ros2 launch mini_rover_description display.launch.py
Open another terminal:
source /opt/ros/jazzy/setup.bash
source ~/ROV---HiL-and-SiL/ros2_ws/install/setup.bash
Then:
ros2 topic list
You should see things including:
/robot_description
/robot_state_publisher
/parameter_events
/rosout
10. Look at the robot in RViz
Open another terminal:
rviz2
In RViz:
Add → RobotModel
Set:
Description Source: Topic
and select:
/robot_description
You should see our little robot.
If it appears correctly, you've just created your first ROS robot description.
11. Now make it actually move
This is where Gazebo enters.
We need to add a differential-drive system.
The modern Gazebo/ROS integration is provided through ros_gz, rather than the old gazebo_ros_pkgs approach you'll see in many older tutorials. The old Gazebo documentation itself warns that those tutorials are for Gazebo Classic and have been superseded. Gazebo Simulator
We want:
              /cmd_vel
                 │
                 ▼
              ROS 2
                 │
             ros_gz
                 │
                 ▼
        Gazebo differential
             drive
                 │
          ┌──────┴──────┐
          ▼             ▼
       left wheel    right wheel
12. Install the required packages
You should already have ros-jazzy-ros-gz.
I'd also install:
sudo apt install \
    ros-jazzy-ros-gz-sim \
    ros-jazzy-ros-gz-bridge \
    ros-jazzy-teleop-twist-keyboard
Now we have:
ROS 2
├── ros_gz_sim
├── ros_gz_bridge
└── teleop_twist_keyboard
13. Don't worry about the full differential-drive implementation yet
There's an important reason I'm deliberately stopping short of throwing a huge SDF/URDF file at you.
For this first exercise, I want you to understand this chain:
keyboard
   ↓
/cmd_vel
   ↓
ROS 2
   ↓
Gazebo
   ↓
wheel velocities
   ↓
robot motion
Once you understand that, the transition to the ROV becomes much easier:
physical HID
      ↓
NATS
      ↓
ROV Control
      ↓
ROS 2
      ↓
Gazebo
      ↓
six thrusters
      ↓
ROV motion
That's the architecture we're actually learning.
14. The next milestone
I'd therefore split the little robot into four small exercises, rather than trying to build everything in one go:
Exercise 1 — Robot description
Xacro
 ↓
URDF
 ↓
RViz
You have the robot visible.
Exercise 2 — Gazebo
Xacro
 ↓
Gazebo
 ↓
robot appears
Exercise 3 — Motion
keyboard
 ↓
/cmd_vel
 ↓
Gazebo
 ↓
robot moves
Exercise 4 — Sensors
Add:
IMU
camera
lidar
and observe:
ROS 2 topics
This is particularly useful for your ROV project because the sensors are where simulation starts becoming interesting.
And then I'd turn it into an ROV
Once the little robot works, we replace:
2 wheels
with:
6 thrusters
and replace:
differential drive
with an ROV 6-DOF motion model:
                  ROV
                   │
        ┌──────────┼──────────┐
        │          │          │
       Surge      Sway      Heave
        │          │          │
       Roll       Pitch       Yaw
        │          │          │
        └──────────┼──────────┘
                   │
              Thruster mixer
                   │
             T1 T2 T3 T4 T5 T6
That is the point where your real ROV Control architecture starts becoming useful.
One thing I would change from our earlier plan: don't put this little robot straight into the existing GitHub repository yet. Keep it in ~/ROV---HiL-and-SiL/ros2_ws as a throwaway learning environment until we have proved the whole ROS/Gazebo chain. Then we can create the proper ROV---HiL-and-SiL repository structure around what we've learned.